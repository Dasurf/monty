#include "ds_monty.h"
/**
 * ds_stack - prints the top
 * @head: stack head
 * @count: line_number
 * Return: nothing
*/

void ds_stack(stack_t **head, unsigned int count)
{
	(void)head;
	(void)count;
	bus.lifi = 0;
}
